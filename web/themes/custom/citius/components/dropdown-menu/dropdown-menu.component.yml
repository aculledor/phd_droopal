schema: "https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata-full.schema.json"
name: Dropdown Menu
description: "A navigation dropdown menu with optional submenus."
status: stable
props:
  type: object
  properties:
    items:
      type: array
      title: "Menu Items"
      description: "Array of menu items for the dropdown."
      items:
        type: object
        properties:
          title:
            type: string
          url:
            type: string
